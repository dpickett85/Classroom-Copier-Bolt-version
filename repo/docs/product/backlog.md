# Product Backlog

## Open (14)
- **[PM]** Real Google integration follow-on: replace the mock Google Classroom + Drive adapter with live APIs — real OAuth (prompt=select_account), consent-screen setup, corrected scope list (add classroom.courseworkmaterials; drive.file for Scenario-3 "Copy to My Drive"), Google app verification / restricted-scope security assessment, and a live E2E transfer between two real courses. (high)
- **[PM]** Granular selection UI: let teachers pick specific topics/posts to copy instead of whole-course-only (v1 copies the entire course's classwork; per-item choices exist only in the pre-flight action sheet). (medium)
- **[PM]** Idempotent re-run / duplicate detection: detect when the same source has already been copied into the same target and offer skip/merge instead of creating duplicate drafts (v1 warns pre-transfer and creates duplicates). (medium)
- **[PM]** Stripe billing activation on the stubbed credit/subscription middleware (deduct only on 100% clean transfer; auto-refund on fallback injections), plus pricing design once free-launch usage data exists. (low)
- **[PM]** OAuth scope-minimization review before Google verification submission: evaluate drive.metadata.readonly + drive.file in place of restricted drive.readonly to reduce security-assessment burden. (low)
- **[UI]** MANUAL-VERIFY: UI design-system sync to Claude Design (claude.ai/design) was declined under Beast Mode's repository-boundary override. If a hosted, browsable design system is wanted, a human should invoke the UI stage's design-sync offer interactively. (low)
- **[ARCHITECT]** Verify empirically whether Render free-tier local disk (SQLite file) survives a genuine redeploy (not just sleep/wake) before treating job-history durability across deploys as guaranteed. Architecture already mitigates the downside via idempotent fixture reseed-on-boot regardless of the answer; if durability across redeploys matters, the pre-decided remedy is Postgres (rejected on cost/ops for v1, named as the upgrade path). (medium)
- **[ARCHITECT]** Formalize a numbered fixture for cold-start behavior (">15 min idle" wake) so QC's certification language can cite it the same way it cites F1-F13. v1 architecture provides a non-fixture, env-gated test harness (COLD_START_SIMULATE_DELAY_MS) instead, which is deterministically testable but explicitly not part of the canonical fixture manifest. (low)
- **[ARCHITECT]** Real-API follow-on must reconcile the mock's QUIZ_ASSIGNMENT workType (no equivalent in the real Google Classroom API's literal workType enum) — likely maps to ASSIGNMENT plus a detected Google Form attachment. Also must account for Classroom's asynchronous write consistency (a created draft may not appear in an immediate subsequent list call) and richer real 429 error envelopes (Retry-After, structured reason fields) that the mock's deterministic simulation does not produce. (medium)
- **[ARCHITECT]** Engineer must choose and implement one concrete narrow-viewport (tablet/Chromebook) responsive pattern for the Completion Summary's 5-tile stat grid and itemized log table before/while building the results screen (carried from UI's Delta; architecture offers non-binding guidance favoring horizontal-scroll-with-sticky-column over card-collapse to preserve the ledger/manifest design concept, but does not mandate it). (medium)
- **[ENGINEER]** Deployment spike (Δ1, critic finding R): deploy both Render services, create a transfer job, let the free instance SPIN DOWN (not only redeploy), wake it, and check whether the TransferJob/TransferJobItem rows survive. Run this BEFORE the resumability guarantee is stated as delivered. Nothing in this build asserts Render's current free-tier filesystem policy — if it is ephemeral, resumability is intra-process only, boot reconciliation has nothing to reconcile (a silent drop at the job level), the idempotent reseed restores the fixture world but NOT job state, and fixture_f12_reconnect_fidelity certifies nothing about the deployed environment. Pre-decided remedy: Postgres. Verify at the same time whether Render's own health-check polling of /api/health prevents the instance from ever idling, which would make the cold-start state unobservable in production (DEFER-1). (high)
- **[ENGINEER]** Formalise a numbered fixture for cold start so QC can cite it the way it cites F1–F14. Today cold start is covered by an env-gated harness (COLD_START_SIMULATE_DELAY_MS) that is deliberately NOT part of the manifest and is verified inert under a production-like NODE_ENV. Also carry the D29 relabel forward: the mechanism is latency-triggered, so UX Acceptance Scenario #8's ">15 min idle" precondition is superseded by "any unresolved call >2s" — update 02-ux-workflow.md's scenario text at the next PM/UX pass so QA is not testing a precondition the product does not implement. (medium)
- **[ENGINEER]** Remaining critic DEFER items and small carried gaps: (a) GET /:id/items?since= for mid-transfer detail — correctly noted-not-built; (b) QUIZ_ASSIGNMENT has no real-API workType equivalent and must be reconciled when RealClassroomProvider ships (likely ASSIGNMENT + a detected Form attachment); (c) session TTL is a 24h default with no product requirement behind it; (d) no fixture combines an attachment failure (F2/F3) with a rubric denial (F7) on the same post, so the combined-outcome rule is implemented and unit-covered but not fixture-tested; (e) route the architect-proposed rate-limit-exhaustion note string through a content pass before ship (Δ2) — F13's gate asserts distinctness, not a literal, so a copy change is a one-line edit and not a broken test. (low)

- **[QA]** QA-6 (cycle 2, verification-scope note) — QA's cycle-2 "no fourth undisclosed divergence found" check (see `06-qa-report.md` § Cycle 2 §5) was targeted at the areas the 16 APPLY findings touched (APPLY-D, G, B specifically), not an exhaustive line-by-line diff of the whole cycle-2 changeset against the mock/real Google Classroom API surface. Not a known defect — a process note that QC or a future pass should treat this as a spot-check, not a certification that no other silent mock/real divergence exists anywhere in the changed files. (low)

## Done (16)
- **[UX]** (2026-08-14) Mid-transfer cancel: POST /transfer-jobs/:id/cancel (idempotent; 409 once finished). Executor drains between items; in-flight item completes; drained items are skipped/skippedByUser via closed SkipReason 'cancelled_by_user' with a centralized note; job lands completed+cancelledAt, no new status; guard//active/reconciler untouched. Reveal-confirm control, cancelled banner, tile attribution. Red-first throughout.
- **[UX]** (2026-08-14) CSV export of the itemized log shipped on the Completion Summary (escaping unit-tested, filename dated, register-conformant label).
- **[UI]** (2026-08-14) Narrow-viewport strategy implemented per the architecture's non-binding guidance: sticky-first-column horizontal scroll for the log (opaque pin, zebra+hover preserved), 5-tile grid 3+2 at 1024px / 2-col at 768px, keyboard-reachable scroll region. Verified in a real browser via the E2E suite.
- **[UI]** (2026-08-14) WCAG AA now browser-verified: axe-core runs per wizard step in real Chromium (e2e/specs/axe-per-step.spec.ts), 0 violations at WCAG 2.1 AA including color-contrast — corroborating the arithmetic token audit with a real layout engine.
- **[UI]** (2026-08-14) Microcopy content pass: generic catch-all error copy rewritten in-register and no longer transfer-specific; scan_stale/scan_already_used get dedicated copy with a re-scan action; rate-limit-exhaustion note content-passed (shared/src/notes.ts; F13 distinctness gate green before and after).
- **[ARCHITECT]** (2026-08-14) Rate-limit-exhaustion note string content-passed and confirmed (one-line change in shared/src/notes.ts as predicted; F13 asserts distinctness so no test churn).
- **[ENGINEER]** (2026-08-14) Real-browser E2E landed: Playwright + @axe-core/playwright, 4 specs / 8 cases on a self-contained server+client (ports 4100/5273) — clean transfer with balanced reconciliation, Action Sheet with the canonical note verbatim, refresh-resume mid-transfer (F12 at browser level), axe per step. 8/8 against the merged tree. wcag_aa_automated_per_step is now browser-verified; the jsdom caveat is CLOSED.
- **[ENGINEER]** (2026-08-14) Cycle-2 DEFERs closed: mock 429 attempt counts persisted in SQLite (survive provider rebuild); duplicate GET /transfer-jobs/active on reconnect eliminated; signOut failure now surfaces a notice instead of proceeding silently.
- **[ENGINEER]** (2026-08-14) Two-process lease harness (test:lease-mp, real child processes over one SQLite file) — and it FOUND A REAL BUG: two executors could both win the claim because 'running' stayed in the WHERE set after the first claim committed. Fixed (executorId: null in the predicate); deterministic across 6 consecutive runs. The synchronous gate had been green throughout.
- **[ENGINEER]** (2026-08-14) scan_stale / scan_already_used surfaced with their own in-register copy and a re-scan action, replacing the generic catch-all for both 409s.
- **[ENGINEER]** (2026-08-14) CSRF closed ahead of OAuth: X-Classroom-Copier custom header required on all state-changing routes (403 without), forcing a CORS preflight on the previously simple-request sign-out. Red-first (csrf.test.ts).
- **[QA]** (2026-08-14) QA-5: 05-implementation.md's P0-1 section corrected to name all three defense layers rather than attributing the guarantee to the evidence-aware catch alone.
- **[ENGINEER]** (2026-08-14) SCHEDULED CourseWorkState divergence resolved ahead of the real adapter: the invented state is removed from the vocabulary (now DRAFT | PUBLISHED, matching Google; DELETED deliberately unmodelled — no fixture exercises it), F8's scheduled post is a DRAFT carrying scheduledTime, ProviderCourseWork/EnumeratedPost carry scheduledTime, and the contract test asserts the real representation as fidelity instead of naming a divergence. One fact, one column. 365 tests green.
- **[QA]** Add a test exercising JobReconciler.start(intervalMs)'s actual setInterval firing (e.g. fake timers) that wedges a job in 'running' with a stale heartbeat and asserts the interval reconciler resolves it WITHOUT a process restart or a direct reconcileStaleJobs() call. This is the specific acceptance gate 04-architecture.md's composition-root module declares ("A second test asserts the interval reconciler resolves a job wedged in 'running' WITHOUT a process restart") and it does not currently exist — every existing test (job-reconciler.budget.test.ts, api.integration.test.ts) calls reconcileStaleJobs() synchronously, which exercises the reconciliation logic but not the interval-scheduling mechanism itself (D12's "not only at boot" half of the fix). (medium) — **RESOLVED at cycle 2** (see 05-implementation.md § Cycle 2, C2.3).
- **[QA]** Add a unit test asserting the combined-outcome rule for outcome=fallback_shell + rubricDegraded=true co-occurring on the same item (02-ux-workflow.md §4 "combined-outcome rule": an item can be both a fallback shell AND rubric-degraded, and must count exactly once under fallback_shell in the reconciliation sum). Currently server/src/services/transfer-engine.test.ts only exercises rubricDegraded against outcome=transferred (F7, F1/D24) — the fallback_shell+rubricDegraded combination is untested even at the unit level, which is narrower than 05-implementation.md's backlog description ("implemented and unit-covered but not fixture-tested"). The code appears correct by construction (rubricDegraded is set independently of outcome in transfer-engine.ts's finish()), but the specific combination has zero test coverage, fixture or synthetic. (low) — **RESOLVED at cycle 2** (see 05-implementation.md § Cycle 2, C2.3).
- **[QA]** 04-architecture.md §8 "Testability" (line ~1184) and the UI-Δ2 Deltas row (line ~1403) still describe the resolution mechanism as "@axe-core/playwright runs on every wizard step in the E2E suite" — this is stale relative to the Vitest+jsdom+axe-core substitution disclosed accurately in the same document's own "Implementation reconciliation (engineer stage)" section. The reconciliation section is the correct, honest account; §8 and the UI-Δ2 row were not updated to match it and should be, so a reader of §8/Deltas alone (without reaching the reconciliation section) would believe real-browser E2E a11y testing shipped. Also: 05-implementation.md's P0-1 claims "a scripted grep confirms zero unresolved citations" but no such script is committed anywhere in the repo (checked package.json scripts and grepped for citation-check tooling) — QA independently re-derived the same true result by hand, but the claim of a repeatable, checked-in script is unverifiable/inaccurate as stated. (low) — **RESOLVED at cycle 2** (see 05-implementation.md § Cycle 2, C2.3).

## Won't Fix (0)
