# Leave unset for local dev — the Vite dev server proxies /api to the backend,
# which keeps dev same-origin so the session cookie actually travels.
# Set to the deployed API origin for the production build.
# VITE_API_BASE_URL=https://classroom-copier-api.onrender.com
